# rollup-vue-webpack
Simple rollup + vue+webpack project

## Usage

```bash
# clone
git clone git@github.com:gongph/rollup-vue-webpack.git

# install
npm install yarn -g
yarn install

# build
npm run dist

# run
npm run serve
```
